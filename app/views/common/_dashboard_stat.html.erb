<article class="stat-card stat-card--<%= tone %>">
  <div>
    <div class="stat-card__label"><%= label %></div>
    <div class="stat-card__value"><%= value %></div>
  </div>
  <span class="stat-card__icon"><%= ui_icon(icon, size: 30) %></span>
</article>
